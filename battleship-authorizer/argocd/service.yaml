apiVersion: v1
kind: Service
metadata:
  name: ms-auth
  namespace: battleships

spec:
  selector:
    app: ms-auth
  ports:
    - name: app
      protocol: TCP
      port: 80
      targetPort: 8000
    - name: metrics
      port: 8080
      targetPort: 8080
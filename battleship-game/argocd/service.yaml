apiVersion: v1
kind: Service
metadata:
  name: ms-game
  namespace: battleships
  labels:
    app: ms-game

spec:
  selector:
    app: ms-game
  ports:
    - name: app
      protocol: TCP
      port: 80
      targetPort: 8000
    - name : metrics
      port : 8080
      targetPort: 8080